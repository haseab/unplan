# Create an OAuth 2.0 Web application in Google Cloud, enable the Google
# Calendar API, and register every environment as an authorized JavaScript
# origin. The browser code model uses this public client ID and the matching
# secret to exchange authorization codes. Refresh tokens are stored in this
# browser's local storage; no database is used.
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
